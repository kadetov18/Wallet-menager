import { ScrollView, Text, View } from "react-native";

export default function ExploreScreen() {
  return (
    <ScrollView style={{ flex: 1, backgroundColor: "#e9e7e7" }}>
      <View style={{ padding: 16 }}>
        <Text>Тут поки порожньо</Text>
      </View>
    </ScrollView>
  );
}
